# 开发记录

## 阶段 1：可启动的最小 3D 版本

- 改动：创建可直接打开的 `index.html`，内置 Three.js，加入开始界面、山门与山路场景、第三人称角色、WASD/方向键移动、鼠标拖动镜头、跳跃与全屏。
- 测试：Playwright 已用 `file://` 直接打开并验证开始页、WebGL 游戏画面、方向键移动和跳跃流程；状态从 `(0, 0, 12)` 移动到约 `(6.06, 0, -3.96)`，没有页面控制台错误。首轮发现方向键组合计算会被未按键的 `undefined` 传播为 `NaN`，已改为显式布尔数值；另补内嵌 favicon，并根据实机截图加宽山门、把沿路树木外移及加入简易镜头遮挡检测。
- 已知问题：任务、战斗、队友、轻功关卡与移动端触控尚未加入；当前美术为程序化占位场景。
- 下一步：先验证最小版本，再实现可通关任务状态机和完整场景路线。

## 阶段 2：完整世界、任务与轻功路线

- 改动：把原型拆为配置、世界、角色、战斗、界面、声音和核心控制模块；完成原创山门、山路、断桥、六个窄落脚点、江边村落与照水寺首领场。
- 改动：加入山门接取、山路清敌、断桥轻功、村民求援、三波寺院防守、首领战、流光返程桥和山门交付的完整状态机。
- 改动：轻功包含疾冲、二段跳和一次踏水宽限；断桥水域没有隐形地面，落水会回最近检查点，返程阶段才出现安全流光桥。
- 测试：`file://` 直接打开后，Playwright 使用真实方向键与 `Enter` 从开始界面移动到执事处并成功接取任务；状态进入 `ROAD_TO_BRIDGE`，没有控制台错误。
- 问题与修复：Three.js 在 `file://` 中把本地图像作为 WebGL 纹理会污染画布；远景改为 CSS 静态背景，保留可见素材且消除跨域错误。
- 问题与修复：浏览器测试的虚拟步进与常规 RAF 会重复推进移动；首次调用 `advanceTime` 后切换为确定性模式，修复测试中越过交互点的问题。

## 阶段 3：五人战斗、首领机制与跨端界面

- 改动：四名 AI 队友分别实现枪卫近身控场、弓手远程输出、医者群疗与持续恢复、影行突袭；所有队友都能造成伤害、承受首领攻击、倒地并重新起身。
- 改动：玩家拥有普通攻击、闪避、回风式和穿云刺；集火会提高队友攻击频率并帮助打断“锁脉夺息”，归队会让队友回到玩家身边。
- 改动：首领实现“断岳回风”“焚脉落印”“锁脉夺息”三类可辨认攻击、地面预警、破招条、失败回血和破招僵直。
- 改动：加入桌面 HUD、四名队友状态、首领与预警条、技能冷却、触控摇杆、手机八个动作按钮、镜头拖动区和竖屏旋转提示。
- 测试：完整浏览器状态测试从接任务运行到 `COMPLETE`；六个踏石全部登记，三种首领攻击全部出现，治疗量为 226，玩家与四名队友伤害均大于 0，控制台错误为 0。
- 测试：不使用秒杀或无敌辅助的首领战回归在 120.5 秒内获胜、没有重置，治疗量为 4387；三波防守在 80.75 秒内通过并保住 1 名村民。
- 测试：844×390 手机横屏摇杆移动 7.2 米、镜头拖动改变视角、八个按钮均可用；390×844 竖屏显示旋转提示且没有横向溢出，两种布局控制台错误均为 0。
- 问题与修复：移动端开始界面的层级会盖住竖屏提示；提高游戏 UI 层级并只在竖屏菜单显示旋转层。
- 问题与修复：近战队友在首领身边过度重叠；为四种职责设置独立战斗站位，同时保持攻击距离和治疗逻辑。
- 问题与修复：AI 原先只更新平面坐标，山路与寺院又具有不同地面高度，导致队友和敌人埋入地面；所有 AI 的生成、移动、闪避、突袭、重置和碰撞分离现都重新贴合世界地表，获救村民在首领战时退到场外。另新增角色碰撞半径与三轮确定性分离，影行突袭固定停在安全攻击距离；镜头遮挡距离改为停在障碍物前。
- 问题与修复：玩家原先会直接穿过墙、村屋、立柱和 NPC；世界现提供圆形与旋转矩形碰撞代理，常规移动和闪避均使用分段扫掠与贴墙滑动。浏览器验证山门后墙、斜向滑墙、闪避、旋转村屋、寺院墙和 NPC 均能正确阻挡。
- 问题与修复：武器与手臂原先是互不关联的独立节点，挥招时会扫进躯干；双臂现使用肩部枢轴，六类武器挂到右手外侧并分别设置握点与攻击姿态。
- 测试：专项穿模回归中，山路 AI 高度随坡面落在 `y=2.81～3.31`，寺院全队与首领稳定在 `y=0.76`，最近两名战斗角色中心距为 2.35 米；控制台错误为 0。
